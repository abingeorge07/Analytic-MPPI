<mujoco model="g1 scene">
  <include file="g1.xml"/>

  <statistic center="0.0 0.0 0.5" extent="1.5"/>

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.1 0.1 0.1" specular="0.9 0.9 0.9"/>
    <rgba haze="0.15 0.25 0.35 1"/>
    <global azimuth="140" elevation="-20"/>
  </visual>

  <asset>
    <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512" height="3072"/>
    <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3"
      markrgb="0.8 0.8 0.8" width="300" height="300"/>
    <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5" reflectance="0.2"/>
  </asset>

  <worldbody>
    <geom name="floor" size="0 0 0.05" type="plane" material="groundplane"/>
    <!-- Side-on camera that tracks the whole-model COM, so a walking robot stays in
         frame for the g1_walk videos (trackcom keeps a fixed offset from the COM).
         Additive only — does not affect dynamics, sensors, or the standup task. -->
    <camera name="track" pos="0 -2.6 1.2" xyaxes="1 0 0 0 0 1" mode="trackcom"/>
  </worldbody>

  <sensor>
    <!-- Added for the CPU port: expose torso site position so the standup task
         can read torso height from sensordata instead of relying on site_xpos
         (which is not exported by mujoco.rollout). -->
    <framepos name="torso_pos" objtype="site" objname="imu_in_torso"/>
  </sensor>
</mujoco>
